const schemaVentas = `
    CREATE TABLE IF NOT EXISTS ventas
        (
            collection_name text,
            collection_url text,
            trades text,
            volume text,
            floor text
        );
`;
